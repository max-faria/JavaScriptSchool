// for (Inicialização; coondição; finalização){
//    codigo a ser realizado
//}

// A condição de inicialização é realizada apenas uma única vez
// Os itens do FOR são todos opcionais mas deve-se manter a mesma estrutura
// ou seja, deicar o campo vazio

let i = 0;
for (alert("Inicializando o for"); i <= 3; i++) {
  alert("i = " + i);
}

let nome = "Max Faria";
for (i = 0; i < nome.length; i++) {
  console.log(nome[i]);
}
